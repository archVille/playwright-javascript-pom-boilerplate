import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  // await expect(page.getByLabel('Συναίνεση', { exact: true }).getByRole('paragraph')).toContainText('Συναίνεση');
  // await expect(page.getByLabel('Δεν συγκατατίθεμαι')).toBeVisible();
  // await expect(page.getByLabel('Συναίνεση', { exact: true })).toBeVisible();
  // await page.getByLabel('Συναίνεση', { exact: true }).click();
  // await page.getByRole('button', { name: 'ΟΧΙ ΤΩΡΑ' }).click();
});